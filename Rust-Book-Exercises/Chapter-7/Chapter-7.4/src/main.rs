mod lib;
use std::{cmp::Ordering, collections::HashMap}; // Nested imports
use std::io::{self, Write}; // Nested imports using self
use std::collections::*; // Glob Operator to import all publicly defined items

fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}
